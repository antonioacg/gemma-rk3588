# Pinned to airockchip/rknn-llm release-v1.2.3 (commit f7df8e5).
# When bumping rkllm-toolkit, update both the wheel URL and the runtime
# version on the board (serving/) — they MUST match. The librkllmrt.so
# bundled with rkllama 0.0.66 is also 1.2.3.
#
# Python version: pin to 3.11 in CI. The wheel is x86_64 Linux only
# (cp39/310/311/312 wheels published; this requirements file targets cp311).
# auto_gptq 0.7.1 fails to build its CUDA extension on cp312 unless
# BUILD_CUDA_EXT=0 is set — workflow does this defensively.
#
# All other pins below are copied verbatim from upstream
# rkllm-toolkit/packages/requirements.txt at release-v1.2.3.

rkllm_toolkit @ https://github.com/airockchip/rknn-llm/raw/release-v1.2.3/rkllm-toolkit/packages/rkllm_toolkit-1.2.3-cp311-cp311-linux_x86_64.whl

numpy>=1.23.1,<=1.26.4
transformers==4.55.2
torch==2.6.0
datasets==4.1.1
pyarrow==21.0.0
tqdm>=4.64.0
sentencepiece==0.2.0
accelerate>=1.0.1,<=1.5.2
protobuf<=4.25.4,>=4.21.6
transformers_stream_generator==0.0.5
einops==0.4.1
scipy>=1.5.4
tiktoken>=0.7.0,<=0.9.0
tabulate==0.9.0
Jinja2==3.1.4
safetensors==0.5.3
colorlog==6.8.2
datamodel_code_generator==0.26.0
jsonschema==4.23.0
flatbuffers==24.3.25
torchvision==0.21.0
pillow==10.4.0
auto_gptq==0.7.1
optimum==1.23.3
jsonlines==4.0.0
timm==1.0.19
easydict==1.13
addict==2.4.0

# Used only by convert.py to pre-download HF weights deterministically
# (avoids transformers cache + load duplication when invoked from CI).
huggingface_hub>=0.23,<1.0
